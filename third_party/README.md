# Third Party

No third-party dependencies are required for Phase 1.

Later phases may optionally add:

- GoogleTest for tests.
- nlohmann/json if manual JSON writing becomes limiting.
- CUDA helper libraries or wrappers.

For now, the code intentionally stays dependency-light.
